---
permalink: /tags.json
---
{
    "tags": [
        {% for tag in site.tags %}
        {
            "name": "{{ tag[0] }}",
            "slug": "{{ tag[0] | slugify }}"
        }{% unless forloop.last %},{% endunless %}
        {% endfor %}
    ]
}
